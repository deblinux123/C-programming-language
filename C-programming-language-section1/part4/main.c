#include <stdio.h>



void printMessage(); // Decloration only

int main(int argc, char **argv)
{
	printf("Linker error in C");
    printMessage(); // dont have eny error from compile the project
	return 0;
}
